---
import { root } from "../utils/collection";
import type { PillarId } from "../site";
import { getLanguage, localizePath, siteText } from "../i18n";

interface Props {
  title: string;
  tone?: PillarId;
  href?: string;
  moreLabel?: string;
  class?: string;
}

const language = getLanguage(Astro.currentLocale);
const { title, tone, href, moreLabel = siteText[language].seeAll, class: className = "" } = Astro.props;
---

<div class:list={["section-head", className]} data-tone={tone}>
  <span class="tick" aria-hidden="true"></span>
  <h2 class="title">{title}</h2>
  <span class="rule" aria-hidden="true"></span>
  {href && (
    <a href={root(localizePath(href, language))} class="more">
      {moreLabel} <span aria-hidden="true">&rarr;</span>
    </a>
  )}
</div>
