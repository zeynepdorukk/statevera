---
interface Props {
  label?: string;
}
const { label = Astro.currentLocale === "tr" ? "Öne çıkanlar" : "Key takeaways" } = Astro.props;
---

<aside class="callout" aria-label={label}>
  <span class="callout-label">{label}</span>
  <div><slot /></div>
</aside>
