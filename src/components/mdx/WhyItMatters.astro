---
interface Props {
  label?: string;
}
const { label = Astro.currentLocale === "tr" ? "Neden önemli" : "Why it matters" } = Astro.props;
---

<aside class="callout" aria-label={label}>
  <span class="callout-label">{label}</span>
  <div><slot /></div>
</aside>
