---
interface Props {
  label?: string;
}
const { label = "The big picture" } = Astro.props;
---

<aside class="callout" aria-label={label}>
  <span class="callout-label">{label}</span>
  <div><slot /></div>
</aside>
